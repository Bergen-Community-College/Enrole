name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Purge style.js from jsDelivr
        run: curl -s "https://purge.jsdelivr.net/gh/Bergen-Community-College/Enrole@main/style.js"
